# unsupported_why_workspace_selectors

## `vp why react --recursive --workspace-root --filter app --json -- --help`

Reject unsupported workspace-root instead of silently discarding it.

**Exit code:** 1

```
npm does not support --workspace-root.
```
