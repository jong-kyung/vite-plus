[[case]]
name = "command_list_npm10"
vp = "global"
serial = true
skip-platforms = ["windows"]
steps = [
  { argv = ["vp", "install"], comment = "should install packages first", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--json"], comment = "should list installed packages", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "testnpm2", "--json"], comment = "should list specific package", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--depth", "0", "--json"], comment = "should list packages with depth limit", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--long", "--json"], comment = "should list packages with extended info", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--parseable", "--json"], comment = "should list packages in parseable format", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--prod", "--json"], comment = "should exclude dev dependencies using --omit dev", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--dev", "--json"], comment = "reject unsupported dev-only listing instead of listing every dependency", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--no-optional", "--json"], comment = "should exclude optional dependencies (uses --omit optional)", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--exclude-peers", "--json"], comment = "should exclude peer dependencies (uses --omit peer)", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--", "--loglevel=warn", "--json"], comment = "should support pass through arguments", continue-on-failure = true },
]

[[case]]
name = "npm_list_dependency_selection"
vp = ["local", "global"]
local-registry = true
skip-platforms = ["windows"]
steps = [
  { argv = ["node", "-e", "const fs = require('node:fs'); const p = JSON.parse(fs.readFileSync('package.json')); p.optionalDependencies = {'yocto-queue': '0.1.0'}; fs.writeFileSync('package.json', JSON.stringify(p));"], snapshot = false },
  { argv = ["vp", "install", "--ignore-scripts"], snapshot = false },
  { argv = ["vp", "pm", "list", "--prod", "--json"], comment = "production listing excludes dev but keeps optional and peer dependencies" },
  { argv = ["vp", "pm", "list", "--prod", "--no-optional", "--exclude-peers", "--json"], comment = "production listing respects both exclusions" },
  { argv = ["vp", "pm", "list", "--dev", "--json"], comment = "dev-only listing fails even with JSON output", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--dev", "--only-projects", "--find-by", "finder", "--json"], comment = "report all unsupported named options together", continue-on-failure = true },
  { argv = ["vp", "pm", "list", "--", "--include=dev", "--json"], comment = "raw native include remains available and lists all four dependency types" },
]
