# unsupported_list_options

## `vp pm list --depth 0 --json -- --help`

Explicit output options must not succeed through Yarn Berry's list Noop.

**Exit code:** 1

```
yarn >= 2 does not support --depth.
yarn >= 2 does not support --json.
```
